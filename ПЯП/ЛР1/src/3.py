print(sum([int(digit) for i, digit in enumerate(input(), start=1) if i % 2 != 0]))

# start

# io`Ввод: число`
# block`сумма = 0`

# for`i, digit in
# enumerate(число,
# start=1)` {
#     if`i % 2 != 0` then(да) {
#         block`сумма += int(digit)`
#     }
# }

# io`Вывод: сумма`
# stop
