start

io(Помещаем
используемые в
макросе регистры
в стек)

for(Следующая 
строка пока 
не закончатся) 
{

for(Следующий 
столбец пока 
не закончатся) 
{

block(Получаем элемент)

block(Копируем в 
другую матрицу
result)
}

}
connect(A)

connect(A)
for(Следующая 
строка пока 
не закончатся) 
{

for(Следующий 
столбец пока 
не закончатся) 
{

block(Получаем элемент)

if(элемент < 0)

else(да) { block(добавляем к сумме 
орицательных 
элементов строки) }

}
if(сумма 
отрицательных 
!= 0)
then(да) { block(записываем её 
в начало строки)}
}

io(Извлекаем
сохранённые в стеке
значения обратно
в регистры)

stop
